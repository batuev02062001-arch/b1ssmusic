[build]
builder = "nixpacks"

[build.nixpacksConfig]
nixPkgs = ["ffmpeg", "python311", "python311Packages.pip"]
installCmd = "python3 -m pip install -r requirements.txt"

[deploy]
startCommand = "python3 bot.py"
